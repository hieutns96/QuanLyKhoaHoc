@model IEnumerable<WebQLKhoaHoc.DSBaiBaoDeTai>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminMainPage.cshtml";
}

<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>
                    Danh sách bài báo về đề tài
                </h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li class="dropdown">
                        <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                        <ul class="dropdown-menu" role="menu">
                            <li>
                                @Html.ActionLink("Create New", "Create", "AdminDSBaiBaoDeTai")
                            </li>
                        </ul>
                    </li>

                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div id="datatable_wrapper" class="dataTables_wrapper form-inline dt-bootstrap no-footer">
                    <!-- list component  -->
                    <div class="row">
                        <div class="col-sm-12">
                            <table id="datatable" class="table table-striped table-bordered dataTable no-footer" role="grid" aria-describedby="datatable_info">
                                <thead>
                                    <tr role="row">
                                        <th>
                                            @Html.DisplayNameFor(model => model.BaiBao.TenBaiBao)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.DeTai.TenDeTai)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.GhiChu)
                                        </th>
                                        <th>Action</th>
                                    </tr>
                                </thead>
                                <!-- grid nkh  -->
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                    <tr role="row" class="even">

                                        <td>
                                            @Html.DisplayFor(modelItem => item.BaiBao.TenBaiBao)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.DeTai.TenDeTai)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.GhiChu)
                                        </td>
                                        <td>
                                            <a href="@Url.Action("Edit", "AdminDSBaiBaoDeTai", new {id = item.MaTT})">
                                                <i class="far fa-edit"></i>
                                            </a>
                                            <a href="@Url.Action("Delete", "AdminDSBaiBaoDeTai", new {id = item.MaTT})">
                                                <i class="far fa-trash-alt"></i>
                                            </a>
                                        </td>
                                    </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


