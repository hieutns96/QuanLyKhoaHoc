@model IEnumerable<WebQLKhoaHoc.HocVi>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminMainPage.cshtml";
}


<div class="row">
    <div class="col-md-12 col-sm-12 col-xs-12">
        <div class="x_panel">
            <div class="x_title">
                <h2>
                    Học vị nhà khoa học
                </h2>
                <ul class="nav navbar-right panel_toolbox">
                    <li class="dropdown">
                        <a href="javascript:;" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                        <ul class="dropdown-menu" role="menu">
                            <li>
                                @Html.ActionLink("Create New", "Create", "AdminHocVi")
                            </li>
                        </ul>
                    </li>

                </ul>
                <div class="clearfix"></div>
            </div>
            <div class="x_content">
                <div id="datatable_wrapper" class="dataTables_wrapper form-inline dt-bootstrap no-footer">
                    <!-- list component  -->
                    <div class="row">
                        <div class="col-sm-12">
                            <table id="datatable" class="table table-striped table-bordered dataTable no-footer" role="grid" aria-describedby="datatable_info">
                                <thead>
                                    <tr role="row">
                                        <th>Action</th>
                                        <th>
                                            Tên viết tắt
                                        </th>
                                        <th>
                                            Tên học vị
                                        </th>
                                        
                                    </tr>
                                </thead>
                                <!-- grid nkh  -->
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr role="row" class="even">
                                            <td>
                                                <a href="@Url.Action("Edit", "AdminHocVi", new {id = item.MaHocVi})">
                                                    <i class="far fa-edit"></i>
                                                </a>
                                                <a href="@Url.Action("Delete", "AdminHocVi", new {id = item.MaHocVi})">
                                                    <i class="far fa-trash-alt"></i>
                                                </a>
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.TenVietTat)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.TenHocVi)
                                            </td>

                                            
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

